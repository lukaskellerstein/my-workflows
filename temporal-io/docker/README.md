```Bash
docker compose -p my-temporal-io up -d
```

Some exposed endpoints:

`http://localhost:8080` - Temporal Web UI
`http://localhost:9200` - Elasticsearch
`http://localhost:8000/metrics` - Server metrics
